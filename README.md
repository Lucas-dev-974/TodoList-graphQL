# Insallation dépendances
    - cp .env.example .env
    - npm install
    - composer install


# Lancement du projet
    - php artisan key:generate
    - php artisan migrate
    - php artisan serve
    - npm run watch (dans un autre terminal que le serve)